name: epictale_telegram
description: An empty Aqueduct application.
version: 0.0.1
author: stable|kernel <jobs@stablekernel.com>

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  http: ^0.12.0

dev_dependencies:
  test: ^1.0.0
  aqueduct_test: ^1.0.0